apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongodb14.fullname" . }}
  labels:
    {{- include "mongodb14.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongodb14.selectorLabels" . | nindent 4 }}
